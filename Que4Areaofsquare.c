#include<stdio.h>
{
int main()

float side,area;
printf("Enter the side of the square: ");
scanf("%f",&side);
area= side * side ;
printf("Area of the square have side %f is %f",side,area);

return 0;
}
